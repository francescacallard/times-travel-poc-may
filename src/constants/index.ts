export const RESPONSE_OK = {
  message: 'OK.',
  statusCode: 200,
}

export const REQUEST_ACCEPTED = {
  message: 'Request Accepted.',
  statusCode: 202,
}

export const RESPONSE_BAD_REQUEST = {
  message: 'Bad Request.',
  statusCode: 400,
}

export const RESPONSE_UNAUTHORIZED = {
  message: 'Unauthorized access.',
  statusCode: 401,
}

export const RESPONSE_UNAUTHORIZED_USER_AGENT = {
  message: 'This user-agent is not authorized.',
  statusCode: 403,
}

export const RESPONSE_NOT_FOUND = {
  message: 'Resource not found.',
  statusCode: 404,
}

export const RESPONSE_VALIDATION_FAILED = {
  message: 'Validation failed.',
  statusCode: 422,
}

export const RESPONSE_SERVER_ERROR = {
  message: 'Server error',
  statusCode: 500,
}
